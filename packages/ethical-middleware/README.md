# @hcai/ethical-middleware

The guardrails. `HumanCheckpoint` is the execution gate; `tripwires` sever agents that breach scope; `signing` issues HMAC approval tokens bound to one specific action hash (forge-proof and replay-proof). See the root README for wiring.
